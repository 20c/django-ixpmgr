[tox]
envlist = py36

[testenv]
passenv = CI TRAVIS*
commands = py.test src/tests --cov-report=term-missing --cov-report=xml --cov={envsitepackagesdir}/src/django_ixpmgr
           codecov -e TOXENV
deps =
  -r{toxinidir}/Ctl/requirements-test.txt

[pytest]
norecursedirs = .ctl data gen .tox
